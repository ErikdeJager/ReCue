---

kanban-plugin: board

---

## Refine

- [ ] Overhaul Kanban card interaction and create/edit UI: clicking/dragging a card always drags it, with edit (pencil) and delete actions only via their buttons; create AND edit use a single input field (card = one sentence, Shift+Enter adds description lines, more elaborate edits left to the raw editor); fix the edit UI so the save/delete buttons no longer overlay the text
- [ ] Add a Settings section to configure default Kanban column colors — let the user choose which color each column gets
- [ ] Make the Kanban "Add card" and "Cancel" buttons a bit bigger with more padding between the text and the button edge

## READY

- [ ] #237 — Persist the diff viewer's display modes (focus/accordion + unified/split) so the last choice becomes the default for new viewers
	Plan: TASK-237.md
	Depends on: none

## DONE


// Shared TypeScript types for ClaudeCue.
//
// Backend-mirrored models (sessions, git diff, persistence records) are declared
// here as later tasks introduce them, so the frontend store and the typed IPC
// layer share a single source of truth.
export {};
